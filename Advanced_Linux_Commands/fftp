It runs the classical command- line file transfer client,FTP.IT's an interactive text user interface for using the APRANET standard file tanfer protocol.
It can be used to tranfer files to and from a remote network.
